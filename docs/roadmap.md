# Roadmap

## Next version target

- Complete validation for the Vite and TypeScript tooling migration:
  - `vite.config.ts`
  - `eslint.config.ts`
  - `scripts/*.ts`
- Keep Node.js and npm as the fixed runtime/toolchain.
- Preserve the current Obsidian plugin output contract:
  - release asset: `main.js`
  - release asset: `manifest.json`
  - CommonJS bundle format
  - externalized Obsidian/Electron APIs
